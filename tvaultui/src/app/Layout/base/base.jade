script(type='text/ng-template', id='text-input.modal.html')
    .modal-sep.createFolder.montserrat
        .common-header-label.text-center {{config.title}}
    form.createFolder.left-lg-sep.right-lg-sep(ng-submit="close({inputValue: form.inputValue, passwordValue: form.passwordValue})")
        .modal-inner-wrapper
            .common-form
                .common-form-row
                    .details-input-group
                        .common-form-label {{config.inputLabel}}
                        input.form-control.input-box-style(placeholder="{{config.placeholder}}" restrict-special-char ng-model="form.inputValue" required)
                .common-form-row(ng-if="config.passwordLabel")
                    .details-input-group
                        .common-form-label {{config.passwordLabel}}
                        textarea.form-control.input-box-style(placeholder="{{config.passwordPlaceholder}}" ng-model="form.passwordValue" required)
        .au-modal-buttons
            .au-inline-buttons
                button.common-btn.btn.btn-primary.btn-lg(type="submit") {{config.submitLabel}}
                button.link-style.common-btn.btn.btn-default.btn-lg(ng-click="dismiss()") {{config.cancelLabel || 'CANCEL'}}

script(type='text/ng-template', id='stop.modal.html')
    .row.montserrat
        .top-lg-sep
            .common-header-label.text-center {{config.title}}
        .top-lg-sep.notification-text {{config.message}}

    .text-center.bottom-lg-sep.top-lg-sep.notification-button
        .clearfix.bottom-sm-sep
            button.link-style.common-btn.btn.btn-primary.btn-lg(ng-click="dismiss()") Close

script(type='text/ng-template', id='confirm.modal.html')
    .row.montserrat
        .top-lg-sep
            .common-header-label.text-center {{config.title}}
        .top-lg-sep.notification-text {{config.message}}
    form.createFolder.left-lg-sep.right-lg-sep
        .au-modal-buttons
            .au-inline-buttons
                button.common-btn.btn.btn-primary.btn-lg(ng-click="close({inputValue: form.inputValue, passwordValue: form.passwordValue})") {{config.submitLabel}}
                button.link-style.common-btn.btn.btn-default.btn-lg(ng-click="dismiss()") {{config.cancelLabel || 'CANCEL'}}


header
sideBar

div.au-base-content(ui-view="content")