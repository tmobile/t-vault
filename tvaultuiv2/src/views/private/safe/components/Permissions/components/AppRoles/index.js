import React from 'react';
// import AddAppRole from '../../../AddAppRole';

const AppRoles = () => {
  // eslint-disable-next-line no-unused-vars
  const handleSaveClick = (role, access) => {};

  return (
    <div>
      {/* <AddAppRole
        handleSaveClick={(role, access) => handleSaveClick(role, access)}
      /> */}
      App Roles
    </div>
  );
};

export default AppRoles;
